; Raise upload limits for the admin (read by PHP-FPM / CGI per directory).
; Photos are also downscaled in the browser before upload, so these are headroom.
upload_max_filesize = 25M
post_max_size = 80M
max_file_uploads = 40
